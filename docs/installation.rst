============
Installation
============

At the command line::

    $ easy_install django-nsync

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-nsync
    $ pip install django-nsync
